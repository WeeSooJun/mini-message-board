extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href="/new" class="btn btn-primary" role="button") Add New Message

  each message in messages
    h4 #{message.user}
    p #{message.text}
    p 
      small #{message.added}
